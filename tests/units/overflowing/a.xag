READ_ME {
A library whose ITMT finds a sum that does not fit in one of its own functions.
Building this alone is refused, which is what ITMT is for.
}
LIBRARY {
    fn.export.int8 'bump' [int8 'n'] { give ['n' + *100*]; }
}
ITMT {
    print.stdout[bump[*100*] \n];
}
